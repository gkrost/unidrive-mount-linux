# Rust build outputs
/target
**/*.rs.bk
*.pdb

# Cargo.lock is committed — this workspace ships a binary crate, so the
# lockfile is tracked for reproducible builds and supply-chain auditing.

# IDE
/.idea
/.vscode

# OS / editors
*.swp
*.bak
*.orig
*~
.DS_Store

# Logs & temp
*.log
*.tmp

# Local secrets / env
.env
.env.local
*.local
